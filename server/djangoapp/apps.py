"""
Django App Configuration
------------------------
This file defines the application configuration for the
`djangoapp` Django application. It allows Django to
recognize and include the app within the project.
"""

from django.apps import AppConfig

class DjangoappConfig(AppConfig):
    name = 'djangoapp'